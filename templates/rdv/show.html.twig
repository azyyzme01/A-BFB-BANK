{% extends 'back/index.html.twig' %}

{% block title %}Rendez-vous {% endblock %}

{% block body %}
<style>
    .bn60 {
  background-color: #190c8d;
  color: #ffffff;
  font-size: 1.3em;
  font-weight: 400;
  position: relative;
  outline: none;
  border-radius: 50px;
  display: flex;
  justify-content: center;
  align-items: center;
  cursor: pointer;
  height: 55px;
  width: 100px;
}
</style>
    <h1>Rdv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rdv.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ rdv.date ? rdv.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Start</th>
                <td>{{ rdv.start ? rdv.start|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ rdv.end ? rdv.end|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Raison</th>
                <td>{{ rdv.raison }}</td>
            </tr>
            <tr>
                <th>Heure</th>
                <td>{{ rdv.heure }}</td>
            </tr>
        </tbody>
    </table>

    <a class="bn60" href="{{ path('app_rdv_index') }}">back to list</a>

    <a class="bn60"  href="{{ path('app_rdv_edit', {'id': rdv.id}) }}">edit</a>
    <style>

        .t1{
         border-collapse: collapse;
         width: 1000px;
         margin-bottom: 400px;
        }
        th,td {
            border: 1px solid black;
            padding: 70px;
        }
        caption{
            background-color: lightblue;
            font-weight: bold;
        }
            </style>
    {{ include('rdv/_delete_form.html.twig') }}
{% endblock %}


