<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Article</title>
    <style>
        h1 {
            text-align: center;
            margin-top: 50px;
            margin-bottom: 50px;
            color: rgb(26, 150, 126);
        }
        table {
            background-color: white;
            border: 2px solid rgb(26, 150, 126);
            border-collapse: collapse;
            width: 70%;
            margin: 0 auto;
            background-color: rgba(0, 34, 255, 0.03);
            border-collapse: collapse;
            font-size: 20px;
        }

        th, td {
            padding: 10px;
            text-align: left;
            border: 1px solid rgb(26, 150, 126);
        }

        .table-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
    </style>
    <h1 class="align-center">A+BFB BANK  </h1>

    <p>Voila la liste de tout les Clients</p>
    <table class="table" border="5" >
        <tbody>
        {% for user in user %}
        <tr>
            <th>ID Client</th>
            <td>{{ user.id }}</td>
        </tr>


        <tr>
            <th>Nom Client</th>
            <td>{{ user.name }}</td>
        </tr>
        <tr>
            <th>Prenom Client</th>
            <td>{{ user.Prenomc }}</td>
        </tr>
        <tr>
            <th>Numéro de Telephone</th>

            <td>{{user.NumTel}}</td>
        </tr>
        <tr>
            <th>EMAIL</th>

            <td>{{user.email }}</td>
        </tr>

        <tr>
            <th>Ville</th>

            <td>{{  user.City }} </td>

        </tr>


        </tbody>





    </table>
{% endfor %}