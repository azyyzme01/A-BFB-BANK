{% extends 'base-back.html.twig' %}

{% block title %}Transaction{% endblock %}

{% block body %}
<style>
    .bn60 {
  background-color: #190c8d;
  color: #ffffff;
  font-size: 1.3em;
  font-weight: 400;
  position: relative;
  outline: none;
  border-radius: 50px;
  display: flex;
  justify-content: center;
  align-items: center;
  cursor: pointer;
  height: 55px;
  width: 100px;
}
</style>
    <h1>Transaction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transaction.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ transaction.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ transaction.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ transaction.email }}</td>
            </tr>
            <tr>
                <th>Num_tlfn</th>
                <td>{{ transaction.numTlfn }}</td>
            </tr>
            <tr>
                <th>Compte_destination</th>
                <td>{{ transaction.compteDestination }}</td>
            </tr>
            <tr>
                <th>montant</th>
                <td>{{ transaction.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a  class="bn60" href="{{ path('app_transactionback_index') }}">back to list</a>

    <a class="bn60" href="{{ path('app_transactionback_edit', {'id': transaction.id}) }}">edit</a>

    {{ include('transaction/_delete_form.html.twig') }}
{% endblock %}
